const calci = require('./basic-calculator');

console.log('typeof calci is', typeof calci);
console.log('calci is', calci);

const a = 12, b = 22;

// console.log(`sum of ${a} and ${b} is ${calci.add(a, b)}`);

// console.log(`difference betwen ${a} and ${b} is ${calci.subtract(a, b)}`);
// console.log(`product of ${a} and ${b} is ${calci.multiply(a, b)}`);
// console.log(`division of ${a} by ${b} is ${calci.divide(a, b)}`);
// console.log(`reminder of ${a} divide by ${b} is ${calci.modulus(a, b)}`);